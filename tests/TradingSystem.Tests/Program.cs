// Placeholder entry point.
//
// If you go with xUnit, you can delete this file and the project's
// <OutputType>Exe</OutputType> - `dotnet test` doesn't need a Main.
//
// If you'd rather write a tiny custom test runner instead of adding xUnit,
// this is where it goes: reflect over OrderBookTests' methods, invoke each
// one, catch exceptions, print PASS/FAIL per method and a summary count.

Console.WriteLine("TODO: run OrderBookTests");
